## What and why

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] Tests added or updated, and `pnpm verify` is green (build, typecheck, test)
- [ ] No `any`, no unchecked `as` casts; a new node semantics comes with a test that pins it to n8n's behaviour
- [ ] Commit messages follow conventional commits (`fix:`, `feat:`, `docs:`, …)
