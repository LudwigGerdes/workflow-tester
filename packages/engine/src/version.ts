/** The n8n release whose node-type bundle and `n8n-workflow` semantics this engine reproduces. */
export const SUPPORTED_N8N_VERSION = '2.10.0';

/** Version of the engine itself; part of generated-case provenance downstream. */
export const ENGINE_VERSION = '0.0.1';
